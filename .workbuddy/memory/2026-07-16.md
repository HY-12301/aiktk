# 2026-07-16 修复记录（爱题库 index.html）

## 本次修改（已提交本地 c76092f，待部署 GitHub Pages）
1. **今日新学不准 + 近7天趋势未更新**：根因为 UTC 时区 bug。`toISOString()` 在 GMT+8 每天 08:00 前返回昨天，导致按天统计错位。
   - 新增 `ldKey()/todayKey()` 取本地日期，替换趋势 `d.h`、今日记录 `todayLog`、连续天数、今日新学等处的时间分桶。
   - 新增 `d.firstSeen` 映射（{date,law}），今日新学改为“今天首次学的题（不重复计复习）”；旧数据在 loadState 中回填为 2000-01-01 避免首日误算；同步合并加 `m.firstSeen=mergeObj(...)`。
2. **连续学习天数**：从统计页（renderProgressExtra）移到刷题页（renderGoalBar），合并进考试倒计时区块显示“🔥 连续 N 天”，并修正时区。
3. **计时器泄漏到自由刷题页**：新增 `hideTimer()`（清计时+隐藏元素），在 doS（自由刷题开始）、showSummary、endExam 调用；组卷/标记刷题计时仍正常。
4. **统计页标题**：导航标签“统计”→“数据”。

## 部署状态
- GitHub Pages 部署被网络阻断：代理 127.0.0.1:62511 当前 connection refused，直连 github.com 超时。代码已 commit（c76092f），待代理恢复后 `HTTPS_PROXY=127.0.0.1:62511 git -c http.version=HTTP/1.1 push origin main` 即可上线。
- jsDelivr CDN 仍自动镜像 main 分支的 all_questions.json.gz。

## 部署更新
- c76092f 已推送上线（远程 main = c76092f，Pages 已验证含 todayKey/hideTimer/firstSeen/“数据”）。
- 网络恢复后推送流程：`HTTPS_PROXY=127.0.0.1:62511 git -c http.version=HTTP/1.1 push origin main`（需 dangerouslyDisableSandbox；`failed to store: 100001` 为无关凭据告警，可忽略）。

## 第二批修复（已上线 commit 814878a）——“做过的题仍显示新题”
根因：新题标签仅看本地 `d.mem[题号]` 是否存在。做过却仍是新题 = mem 丢失/未写入，三个来源已全部修复：
1. **跨设备 mem 未合并（主因）**：`pullGist` 合并门槛过窄（仅看 t/w/todayLog/examScores），本地 t 不小于远端时不合并→仅在另一设备做过的题 mem 不进本地。改为把 mem/done/notes/flags/firstSeen 记录数也纳入触发条件（合并为非破坏性并集，门槛宜宽）。
2. **清空错题误删记忆**：`clearErrors` 原本 `delete d.mem[id]`，退回新题。改为只清 `d.w`，保留 mem，并加 debounceSync。
3. **组卷模式中途退出丢进度**：paper 模式 mem 仅在 endExam 交卷时写。新增 `EXAM_COMMITTED` 集合 + `commitExamQ(q)`，在 nextQ/skipQ/prevQ 离开已作答题时实时回流（firstSeen/done/错题/mem/kw），每题仅回流一次；endExam 循环跳过已回流题避免 SM-2 重复推进，并补齐 firstSeen 与正确题的 done。

## 注意
- 未改 `mem.nextReview` / 导出文件名里的 toISOString（与本次反馈无关，低风险）。
- 用户偏好：先讨论方案、等“执行”再改代码；部署需显式确认。

## 第三批修改（已上线 commit c7eaaba）——AI知识点稳定 + 首页来源进度分列
1. **AI 知识点每次刷到都变**：`callDeepSeek` 每次提交答案都重新抽 topic 并**无条件覆盖** `d.topicFixes[题号]`；而防重复缓存 `aiCache` 是内存变量，刷新/重开即清空→再次请求 DeepSeek(temp=0.5 非确定性)抽出新词覆盖库值。
   - 修复：加“只生成一次”保护——仅当 `topicFixes[题号]` 为空才写入并迁移 kw；已存在则复用库值、绝不覆盖。写库+Gist 同步逻辑保留，跨设备/刷新一致。（按用户要求：不加“重新生成”按钮）
2. **首页自由刷题卡片进度分列**：卡片新增 `free-prog` 两列，显示「已做真题 X/真题总数」「已做模拟 X/模拟总数」+ 进度条。
   - 分母(真题/模拟总数)与分子(已做数)均来自题库 Q 按 source 统计；`updateHomeProgress` 中 Q 载入后遍历计算。
   - 后台静默预载题库（`if(!D) loadData(...)` 在 init 触发）以在未刷题时也填充分母；未载入时显示“加载中”。
   - 来源值取 `q.source`：'真题' / '模拟题'。

## 第四批修改（已上线 commit fb8a7e5）——AI知识点不限字数 + 同步不覆盖题库说明
1. **AI 知识点去字数限制**：用户要求 topic 不限制字数、只要求简洁。
   - 提示词第1476行去掉“（不超过8字）”，改为“用最简洁的语言概括本题核心考点（力求简短，几个字到十几个字即可，不要啰嗦）”。
   - 写入块第1493-1494行去掉 `topicFix.length<=16` 守卫与 `substring(0,8)` 截断，仅保留 `topicFix.split('.')[0].trim()`（取首句并去空格）。“只生成一次、已存在不覆盖”逻辑不变。
## 第五批修改（已上线 commit 516c5bf）——首页进度改为各科分列
- 用户要求：进度按**各科目**分列（不要总计）、**不要文字**、**只保留数字**、**真题在前模拟题在后**。
- 移除首页卡片的"总计"区块（freeProg：已做真题/模拟总数 + 文字标签）。
- 科目标签模板（subjTags）每科改为两列数字：真题 `d/t`（.sp-zt 橙色 #ff7043）在前，模拟题 `d/t`（.sp-mc 绿色 #43a047）在后，无文字标签；分母加载前显示"加载中"。
- `updateHomeProgress` 重写为一次遍历 Q 统计 每科×来源(真题/模拟) 的 总数t 与 已做d（done[q.id] 存在即算已做，不论对错），写入 ztD/ztT/mcD/mcT 四个 id。
- 旧的 `.subj-info/.subj-bar/.subj-fill/.subj-num` 及 `.free-prog` 相关 CSS 已弃用（保留无害）。

## 第六批（已提交 23d975e，部署中）——知识点规范化合并 + 一次性整理
- 用户确认：① Gist 1MB 容量实测足够（gzip+base64：1000题0.018MB、5000题0.08MB、全库1.7万题仅0.26MB），topic 不是瓶颈；② 要做规范化合并；③ 在设置里做一次性「整理知识点」按钮（不要每题按钮）。
- 新增 `d.topicVocab`（规范考点数组）与 `d.topicAlias`（原始文本→规范文本映射）。
- `canonTopic(raw)` 经 alias 归一；`topicKey`/`topicName` 改为对 base 取 `canonTopic`（修正了原对 q.topic 点号处理 bug）。
- `callDeepSeek` 写入块改存 `canonTopic(topicFix)`，新生成 topic 也走归一。
- `consolidateTopics()` 一次性整理：阶段1 并发(4)重新调 `aiTopicOnly(q)` 生成全部已有 topicFixes（修复旧版截断），逐步写回可断点续；阶段2 分批送 `aiNormalize(list)` 合并同义，构建 alias+vocab，重写 topicFixes 为规范串，重映射 `kw` 键(law.raw→law.canonical 聚合)。带进度弹窗、可取消、失败保留原值。
- 设置面板(openSyncSettings)新增「整理知识点（重新生成并合并）」按钮。
- `pullGist` 合并新增 `m.topicAlias=mergeObj` 与 `m.topicVocab` 数组去重合并。
- 离线逻辑自测通过：4 个同义 kw 键正确合并为 3 个规范键。

2. **同步不会重置 topic（已向用户解释）**：
   - 题库文件 all_questions.json.gz 在 App 中是**纯只读**（doFetch 仅拉取、idbPut 仅本地缓存、Q=d 读内存），App 无任何写回服务器的逻辑；只有我改数据时用 git push 才更新。
   - AI topic 存在个人进度 `d.topicFixes[题号]`（localStorage），经 Gist pullGist/cloudSyncPush 在设备间同步；同步只传进度对象 d，不含题库，故不会覆盖网站题库或重置 topic。`topicFixes` 用 mergeObj 并集合并，已生成的不会被对端清空。
