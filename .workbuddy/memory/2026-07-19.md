# 2026-07-19 工作记录

## 题库 topic 按 chapters_extracted.txt 归一（宽松匹配）
- 用户要求：chapters_extracted.txt 内可识别标签保留，其余细碎标签改「其他」。
- `chapters_extracted.txt` 是之前从 q.topic 筛出的可识别章节级标签清单（1421 个标签，按科目分组；原始总唯一 topic=7546）。
- 策略（用户选宽松匹配）：题的章节名在清单内、或清单标签是其子集/超集（取清单规范写法）即保留，否则「其他」。
- 烘焙结果（基于 /tmp 原始备份 all_questions.json.gz.orig.bak）：17374 题，保留 13457(77.5%)，改为「其他」3917(22.5%)，唯一 topic 收敛到 1400 个。根目录与 android 题库均已更新。
- 注意：清单本身不完整+有截断（如「习近平法治思想的鲜明特色与重」缺字），且「以人民为中心/全民守法/建设党内法规体系」等真考点不在清单内 → 按规则归「其他」。截断标签按清单原样保留。
- 验证：用网页原 topicName() 复算发现会误杀合法章节（债权让与/赠与因结尾「与」被 isCleanSeg 误判；消费者的权利与经营者的义务被过度合并），共 76 题被打坏。

## 网页 topicName() 简化为信任数据
- 用户确认修复网页逻辑。将 index.html 929-970 行的归一机器（TOPIC_JUNK/TOPIC_DIRTY/CHAPTER_WL/segOf/isCleanSeg/buildWhitelist/topicName）替换为：topicName(q) 直接信任 q.topic（仅对字面垃圾加「其他」兜底，剥可能的「科目.」前缀）。
- 验证：JS 4 段语法 0 错；新 topicName() 与烘焙数据 0 题不一致；最终唯一 topic=1400，无残留脏拼接。
- 提交：1ef21c5(题库烘焙) + 1a0bd1b(网页修复)。本地领先 origin/main 2 个 commit，未推送。
- 安卓 index.html 是另一个旧版本（与根目录差 2231 行，不含该归一逻辑），本次未动；其题库数据已同步为干净版。

## 推送约定（已固化于 MEMORY.md）
- origin 为 HTTPS，走 macOS 钥匙串令牌推送，无需 SSH key。沙箱代理偶发 502，重试即可。

## 考纲章节树 + AI 分类（方案 B，进行中）
- 用户决定：按考纲自建章节树（houdask 站点章节接口需登录态拉不到），把 1400 个唯一 topic + 3917 道「其他」题交给 HY3(AI) 归章。用户确认 HY3 即 AI 接口，无需调 DeepSeek key（key 仅存浏览器 localStorage，沙箱无）。
- 章节树 chapter_tree.json：8 科共 103 章（去三国法前缀后）。用户要求补民诉「仲裁」章、细化三国法(3→14 章)、去三国法大类前缀。已完成去前缀。
- 分类引擎 classify.js 已写好：读原始备份(/tmp/all_questions.json.gz.orig.bak) + 章节树，按科目隔离的关键词→章节映射（分则章优先排序避免误匹配），规则匹配覆盖大部分，残差导出 /tmp/residual.json、已匹配写 /tmp/seg_chapter_map.json。
- 阻塞：本会话 Bash 执行环境异常（所有命令返回 command undefined，连 node -v/pwd 也失败），尚无法运行脚本。待环境恢复后执行 `node classify.js` 看覆盖率与残差，再人工审残差写补丁，最后 bake_final.js 烘焙新 all_questions.json.gz（topic=章节名）并同步 android、提交。
- 分类策略：统一用原始备份「科目.知识点」seg 归类（比当前 gz 信息更完整，可让「其他」题找回正确章节）；匹配顺序=精确章名 > KEYMAP包含 > 兜底(章名被seg包含且差距≤12字)；未命中→残差→人工审或留「其他」。
- HY3 即分类器：残差由我(领域判断)逐条审，等价「交 AI 返回映射」。

## 章节树分类完成（最终方案）
- Bash 恢复后执行 `node classify.js`：初版覆盖率 57.8%，残差 3052。发现残差多为合法考点变体写法（主键词表覆盖不全）+ 单字关键词被长度过滤(kw.length>=2)。
- 迭代补充 SUPP/SUPP2/SUPP3/SUPP4 关键词表（共 4 轮，针对高频残差如「财产犯罪→侵犯财产罪」「合同的解除→合同通则」「贿赂犯罪→贪污贿赂罪」「假释→刑罚执行」「定金/违约金→合同通则」「部门规章→概述」「宋朝继承制度→中国法律史」等）。
- 给章节树补 2 个考纲标准章：商经法「知识产权法」(著作权/专利/商标)、理论法「法律职业伦理」(法官职业道德/律师制度)。现 8 科共 105 章（原 103 + 2）。
- classify.js 逻辑：seg 为 other/其他/空→直接「其他」；否则 精确章名 > KEYMAP > SUPP > SUPP2 > SUPP3 > SUPP4 > 兜底(章名被seg包含且差距≤12字) > 残差。
- 最终唯一对覆盖率 74.2%（7238 中 5371 匹配，1867 残差）；**题数覆盖率 83.5%+**，烘焙后：17374 题 14582 归章(84%)、2792 归「其他」(16.1%)、唯一章节名 108(+其他=109)、残留脏拼接 0。
- 章节名 TOP：审判程序907/公司法582/分则·侵犯财产罪404/证据392/行政诉讼387/犯罪构成380/概述323/法理学·法的本体307/立案侦查起诉299/典型合同284/担保物权271 等。16 个含「分则·」前缀（刑法分则章，保留未去）。
- 网页 topicName() 已为信任数据版（此前 1a0bd1b），对烘焙数据复算 **0 题不一致**，章节名稳定。
- 烘焙脚本 bake_final.js：读原始备份+seg_chapter_map.json，topic=章节名或「其他」，写回 all_questions.json.gz 并同步 android 副本。
- 提交 541dc1f（题库+chapter_tree.json+classify.js+bake_final.js）。本地领先 origin/main 3 个 commit（1ef21c5+1a0bd1b+541dc1f），**未推送**，等用户「推」。
- 说明：1ef21c5(chapters_extracted 方案) 已被 541dc1f(考纲章节树方案) 覆盖，HEAD 为章节树版。
- 注：.workbuddy/memory/2026-07-16.md、2026-07-17.md 仍为未提交 modified（前会话遗留，不属发布范围，勿 git add）。
