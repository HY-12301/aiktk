# 2026-07-09

## 算法优化
- 重写出题算法：分层抽样替代纯记忆排序（overdue≤15%+weak≤25%+unseen≤35%+seen≤10%）
- 新增大部头 `topicKey()` 知识点识别，正则匹配率从 77.7% → 100%
- 新增 `upKW()` 追踪每个知识点的错误率和最近练习时间
- 新增 `kwWeakness()` 薄弱度公式：错误率×(1+距上次练习天数/14)
- 重写 `rWt()` 混合权重：知识点薄弱 60% + 记忆逾期 40%
- 交错排列 + 同主题隔离
- 云端同步新增 kw 字段合并

## Bug 修复
- 错题去重：rW() 和 spot-check 两处 push 前加判重
- 暗夜模式：从 `prefers-color-scheme` 改手动 toggle（夸克兼容）
- 错题本去重：两处 push 位置都加了判重检查

## 部署
- 使用 CloudStudio 部署
- Git 提交记录完整
